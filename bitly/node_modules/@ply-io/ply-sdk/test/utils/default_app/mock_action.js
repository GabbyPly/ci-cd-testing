const sample_data = {
	success: true
};

const doSomething = (app) => {
	console.log('do something');
	return sample_data;
};

module.exports = {
	key: 'fake_integration',
	noun: 'Fake',
	title: 'Fake integration',
	type: 'read',
	params: [
		{
			key: 'fake_param',
			name: 'fake',
			type: 'integer',
			required: true
		}
	],
	handler: doSomething,
	sample: sample_data,
	ac_mappings: {}
};

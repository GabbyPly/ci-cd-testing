const utils = require('./utils');
const PARAM_TYPES_MAP = require('./param-types');

const formats = Object.entries(PARAM_TYPES_MAP).reduce((obj, [key, value]) => ({ ...obj, [key]: value.formats }), {});

module.exports = {
	types: PARAM_TYPES_MAP,
	paramFormats: formats,
	utils
};

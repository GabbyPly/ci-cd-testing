const buildUserMatchingResponse = (users, groups = undefined) => ({ users, groups });
const buildUser = (id, name, email) => ({ id, name, email });
const buildGroup = (id, name, users = [], parent = null) => ({ id, name, users, parent });

module.exports = { buildUserMatchingResponse, buildUser, buildGroup };

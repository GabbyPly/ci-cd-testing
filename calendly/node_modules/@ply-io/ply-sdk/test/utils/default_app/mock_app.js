const mock = require('./mock_action');
const actions = {
	[mock.key]: mock
};
const auth = {
	type: 'none'
};

const App = {
	type: 'ply',
	auth,

	beforeMiddlewares: [],
	afterMiddlewares: [],

	triggers: {},
	actions,
	contextsMapping: []
};

module.exports = App;
